t b
r  w  a
r+ w+ a+


fopen() / fclose() 꼭 해줄 것. fclose()를 해야 닫고 저장이 됨.

t는 text모드 읽기, b는 바이너리 모드로 읽기.

r은 읽기만, w는 쓰기만(단 파일부재시 생성), a는 덧붙이기(단 파일부재시 생성)

r+은 읽고 쓰기(단 파일없으면 생성x)
w+는 읽고 쓰기(단 파일없으면 생성o)
a+은 덧붙이기 (단 파일없으면 생성o)

r+과 w+의 차이는 새로운파일도 생성하느냐 안하느냐 차이.
